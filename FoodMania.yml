server:
  rootPath: /api/*
  applicationConnectors:
  - type: http
    port: 10000
  adminConnectors:
  - type: http
    port: 10001

# Database settings.
database:

  # the name of your JDBC driver
  driverClass: com.mysql.jdbc.Driver

  # the username
  user: root


  # the JDBC URL
  url: jdbc:mysql://ekl-lm-perf-platform-xlarge-004.stage.nm.flipkart.com:3306/foodmania
  properties:
    hibernate.dialect: org.hibernate.dialect.MySQLDialect
    hibernate.show_sql: true